<manifest 
    android:versionName="0.1"
    android:versionCode="1"
    package="net.danross.toptask"
    xmlns:android="http://schemas.android.com/apk/res/android"
>

    <uses-sdk android:minSdkVersion="7"></uses-sdk>

    <application android:icon="@drawable/app_icon" android:label="@string/app_name">
        <activity android:label="@string/app_name" android:name=".GaeHomeScreen">
            <intent-filter>
        <action android:name="android.intent.action.MAIN"></action>
        <category android:name="android.intent.category.LAUNCHER"></category>
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity"></activity>
        <activity android:name=".MainActivity"></activity>        
        <activity android:name=".EditTaskActivity"></activity>
        <activity android:name=".ViewTaskActivity"></activity>
        <activity android:name=".AddUserActivity" />
		<activity android:name=".GetAllUserActivity" />
        <!-- <activity android:name=".GaeHomeScreen" />-->

    </application>

	<uses-permission android:name="android.permission.INTERNET" />    

    <supports-screens
        android:largeScreens="true"
        android:normalScreens="true"
        android:smallScreens="true"
        android:anyDensity="true" />
</manifest>
